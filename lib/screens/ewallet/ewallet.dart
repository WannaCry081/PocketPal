import 'package:flutter/material.dart';

class EWalletView extends StatelessWidget {
  const EWalletView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("E-Wallet Page"),
      ),
    );
  }
}